node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Secrets & signing keys — never commit these
*.pem
*.key
libresearch-extension-key.pem

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
/playwright-report/
/test-results/

# Paraglide lockfile (auto-generated, keep compiled output committed)
# The compiled messages in src/lib/paraglide/ are committed so type-check
# and CI work without a separate compile step. The lockfile is machine-specific.
project.inlang/cache

.env.local
.claude
.agents
.codegraph

support.libresearch.ca
starter-for-svelte/
starter-for-svelte/

src/routes/account
src/routes/auth

CLAUDE.md
.claude
.codegraph
.claude-flow